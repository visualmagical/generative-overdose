import  React from 'react';
import st from './styles.module.css';

const Logo = () => {
    return (
        <h1 className={st.logo}>
            generative
            overdose
        </h1>
    );
}

export default Logo;