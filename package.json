{
  "name": "generative-overdose",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && yarn start",
    "deps": "yarn && (cd client && yarn)",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "https": "^1.0.0"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
